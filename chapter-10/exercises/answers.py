# ============================================================
# 第十章 简答题 & 计算题
# 在每个变量中填入你的答案，然后运行 run_tests.py 判题
# ============================================================

# --------------------------------------------------
# 习题 1: 对于 Fig. 10.6 的朴素归约内核，1024 个元素，warp 大小 32，
#          第五次迭代时有多少个 warp 存在控制分歧？（填整数）
#
# 提示：1024 个元素需要 512 个线程，共 16 个 warp。
#       每次迭代的 stride 依次为 1, 2, 4, 8, 16...
#       条件为 threadIdx.x % stride == 0
# --------------------------------------------------
ex1 = None  # TODO

# --------------------------------------------------
# 习题 2: 对于 Fig. 10.9 的收敛型归约内核，1024 个元素，warp 大小 32，
#          第五次迭代时有多少个 warp 存在控制分歧？（填整数）
#
# 提示：stride 依次为 512, 256, 128, 64, 32...
#       条件为 threadIdx.x < stride
# --------------------------------------------------
ex2 = None  # TODO

# --------------------------------------------------
# 习题 3: 将 Fig. 10.9 的内核修改为反向访问模式。
#          在反向版本中，最终结果存储在哪个线程？
#     选项:
#       "A" - threadIdx.x == 0
#       "B" - threadIdx.x == blockDim.x - 1
#       "C" - threadIdx.x == blockDim.x
#       "D" - threadIdx.x == stride
# --------------------------------------------------
ex3 = None  # TODO

# --------------------------------------------------
# 习题 4: 将 Fig. 10.15 的粗化求和归约改为求最大值归约，
#          需要将哪些操作替换？
#     选项:
#       "A" - 仅将 += 替换为 fmax
#       "B" - 将 += 替换为 fmax，atomicAdd 替换为 atomicExch + fmax
#       "C" - 仅将 atomicAdd 替换为 atomicMax
#       "D" - 将 += 替换为 fmax，atomicAdd 替换为 atomicMax
# --------------------------------------------------
ex4 = None  # TODO

# --------------------------------------------------
# 习题 5: Fig. 10.15 的粗化归约内核要支持任意长度输入，
#          需要添加什么关键检查？
#     选项:
#       "A" - 仅在粗化循环中检查 i + tile*BLOCK_DIM < length
#       "B" - 在初始加载和粗化循环中都检查索引 < length
#       "C" - 仅在初始加载时检查 i < length
#       "D" - 不需要额外检查
# --------------------------------------------------
ex5 = None  # TODO

# --------------------------------------------------
# 习题 6: 对输入数组 [6, 2, 7, 4, 5, 8, 3, 1] 执行并行归约
#
# 6a. 使用 Fig. 10.6 的朴素内核，第一次迭代后数组内容？
#     （填列表，例如 [8, 2, 11, 4, 13, 8, 4, 1]）
# --------------------------------------------------
ex6a = None  # TODO

# 6b. 使用 Fig. 10.9 的收敛型内核，第一次迭代后数组内容？
#     （填列表，例如 [11, 10, 10, 5, 5, 8, 3, 1]）
ex6b = None  # TODO
